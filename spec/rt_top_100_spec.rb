require "spec_helper"

RSpec.describe RtTop100 do
  it "has a version number" do
    expect(RtTop100::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
